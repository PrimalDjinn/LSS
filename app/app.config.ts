export default defineAppConfig({
  ui: {
    colors: {
      primary: "amber",
      neutral: "slate",
    },
    button: {
      defaultVariants: {
        color: "primary",
      },
    },
    icons: {
      loading: "i-lucide-loader-circle",
    },
  },
});
